[package]
name = "async-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.47.1", features = [
  "fs",
  "io-util",
  "sync",
  "time",
  "macros",
  "rt-multi-thread",
] }
